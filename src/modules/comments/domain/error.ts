export enum ErrorMessage {
  POST_OR_COMMENT_NOT_FOUND = 'Post or comment not found',
  POST_IS_NOT_FOUND = 'Can not find the post to add the comment',
  COMMENT_IS_NOT_FOUND = 'Comment is not found',
  COMMENT_REQUIRES_EXISTING_POST = 'Parent comment requires an existing post',
}
